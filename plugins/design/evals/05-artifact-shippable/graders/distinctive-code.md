---
type: llm
focus: full_transcript
weight: 3
---
The user asked for a self-contained HTML hero artifact for a warm, editorial
note-taking app, explicitly "distinctive, not generic".

Score PASS if the response produces real, self-contained HTML/CSS that honors the
anti-slop doctrine in the actual code: warm off-white surface + near-black ink (not
pure #fff/#000), a specific accent (not blue/purple gradient), real type with weight/
size contrast (a real linked/named typeface, not one-weight Inter), an intentional/
editorial composition (not centered-everything with three glass cards), depth from
borders/warmth rather than backdrop-filter on everything, and reasonable
responsiveness/accessibility. It should read as one committed idea.

Score FAIL if it outputs the generic template (centered hero, blurple gradient, glass
cards, Inter, pure white) or produces no real self-contained code.
